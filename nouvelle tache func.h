#include <stdio.h>
#include <stdlib.h>
void nouvelleTache(){
	typedef struct{
		char titre[40];
		int ID;
		char desc[300];
	}task;
	//ajouter une nouvelle tache.
	task [1000]
	 for( i=0;i<1000;i++){
	 	//ID.
	 	task[i].ID=1
	 	//le titre.
	 	printf("donner un titre a la tache %d.",i+1);
	 	fgets(task[i],titre,40,stdin);
        //discreption.
        printf("ecrire une discreption a la tache %d.",i+);
	 	fgets(task[i],desc,700,stdin);
        
	 }
	
	
}
